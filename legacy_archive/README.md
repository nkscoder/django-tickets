# Archived integration code

This folder is **not loaded** by the `tickets` Django app. It is kept only as a private reference for teams migrating from an older, host-specific deployment.

Do not import these files in `INSTALLED_APPS`, `urls.py`, or migrations for new open-source installs.

For new projects, use the generic core in the parent package only.

# Legacy migrations (archived)

These migrations are for **existing databases** that were created with an older, customized deployment.

For **new open-source installs**, use only `tickets/migrations/` (run `makemigrations` then `migrate`).

Do not mix both migration trees on the same database.

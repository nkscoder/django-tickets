# Archived templates

These HTML files are **not used** by the generic `tickets` URLs. They are kept only as reference from an older customized UI.

Active templates live in `templates/tickets/` (`*_core.html`, `ticket_detail.html`, `dashboard.html`, etc.).
